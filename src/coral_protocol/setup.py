from setuptools import setup, find_packages

setup(
    name="coral_protocol",
    version="1.0.0",
    packages=find_packages(),
    description="Coral Protocol for agent communication",
    python_requires=">=3.8",
)
